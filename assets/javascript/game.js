// capture user input
// When you click on any of the four crystals, it will generate a number between 1-12 that is hidden to the user
// To add up to the random number generated by computer between 19-120
// If number generated by user is equal to number generated by computer, user wins and game starts over
// If number generated by user is not equal to the number generated by computer, user looses and game starts over
var wins =0;
var loses =0;
var userTotal =0;
var userGuess =0;
var computerGuess =0;
var numbers =[];


// reset number to zero
counter = 0;

//reset numbers array
numbers =[];

// go through the array to generate random number
for (var i=0, t=4; i<t; i++) {
	numbers.push(Math.floor(Math.random() * 12))
}

//generate the cyrstal images
 for (var i=0; i< numbers.length; i++){
        var imageCrystal = $('.container');
        blue.attr('data-num', numbers[i]);
        green.attr('alt', 'crystals');
        red.addClass('crystalImage');
        $('#crystals').html(imageCrystal);

// new number computer generates
computerGuess = Math.floor((Math.random() * 100) + 20);

$(document).ready(function()) {
	newgame();

		// get crystal image 
      $(document).on('click', '.crystalImage', function(){
  		userGuess = userGuess + parseInt($(this).data('num'));
        $('#number').html(userGuess);
		} 
			else if ( userGuess > computerGuess){
				$('#loses').html(Loses);
       	}

       		$('.btn').on('click', function(event) {
        // rerun the new game function on click of the .btn
        newGame();
			});
		});